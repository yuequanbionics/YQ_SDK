Usage: ./Tool.sh [option]
Options:
  make       创建build目录并构建项目
  clean      删除build目录及内容
  completion 配置自动补全功能（用户级，永久生效）

make output -> bin


sudo apt install g++-9
sudo ln -s /usr/bin/g++-9 /usr/bin/g++